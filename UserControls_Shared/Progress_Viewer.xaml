<UserControl x:Class="UserControls_Shared.Progress_Viewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserControls_Shared"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
       <UserControl.Resources>
        <Storyboard x:Key="RotateStoryboard" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="rotateTransform" 
                 Storyboard.TargetProperty="Angle" 
                 To="360" 
                 Duration="0:0:2"/>
        </Storyboard>
    </UserControl.Resources>
    <Grid>
        <Ellipse Width="300" Height="300" Fill="Green"/>
        <Ellipse Width="100" Height="100" Stroke="Blue"  StrokeThickness="5" StrokeDashArray="7">
        <Ellipse.RenderTransform>
            <RotateTransform x:Name="rotateTransform" Angle="0" CenterX="50" CenterY="50"/>
        </Ellipse.RenderTransform>
        </Ellipse>
        <TextBlock Name="CountdownText" FontSize="24" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,10" Text="30"/>
    </Grid>
</UserControl>
